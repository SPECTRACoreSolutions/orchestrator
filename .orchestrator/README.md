# Orchestrator Internal State

This directory contains orchestrator's internal execution state:
- `manifests/` - Activity execution manifests
- `history/` - Activity execution history  
- `cache/` - Temporary cache files

**DO NOT COMMIT** - This directory should be in `.gitignore`

This is the orchestrator's private workspace, separate from the SPECTRA workspace root (`.spectra/`).

